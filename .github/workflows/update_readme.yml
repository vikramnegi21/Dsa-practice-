name: DSA Forge Advanced Sync

on:
  push:
    paths:
      - "problems.csv" # Trigger whenever problems are added/modified
  workflow_dispatch:          # manual trigger from GitHub UI
  schedule:
    - cron: "30 18 * * *"    # daily at 00:00 IST (18:30 UTC)

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # Install dependencies if you add them, but urllib is built-in
      # - name: Install dependencies
      #   run: pip install requests

      - name: Run generate_readme.py
        run: python generate_readme.py

      - name: Commit & push updated files
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md heatmap.svg targets.svg
          git diff --cached --quiet || git commit -m "chore: fully advanced DSA stats portfolio [skip ci]"
          git push
          
